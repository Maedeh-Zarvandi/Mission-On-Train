#include "train.h"
#include<QLabel>
#include"Game.h"
#include <QGraphicsScene>
extern Game * game;
train::train()
{
x=0;
y=0;
//trn = scene->addPixmap(QPixmap(":/new/prefix1/tr.png").scaled(800,60));
}
